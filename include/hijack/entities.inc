/*
 * Blahblahblah something-something perl is a thing that exists only in theory
 * 
 * A stub version of what `libhubbub/build/make-entities.pl` would produce
 */
 
static int32_t dict_root = 0;

static hubbub_entity_node dict[] = {
	{0, -1, -1, -1, 0}
};
